# Dashboard

Capture from `make dashboard` (`http://localhost:8501`), one shot per tab:

1. **`kpi-overview.png`** — the top KPI row with market/day filters applied,
   including the provisional-window warning if any row is inside its 3-day
   attribution window.
2. **`metric-1-coding-rate.png`** — the agent/campaign coaching table.
3. **`metric-2-3-paid-post-call.png`** — paid-post-call rate and value
   recovered, with the `*` estimated-FX marker visible.
4. **`metric-4-inbound-drivers.png`** — the level-1→2→3 drill-down.
