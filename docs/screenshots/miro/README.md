# Miro

**`data-infrastructure-flow.png`** — the architecture flow diagram (source
extracts → ingestion → raw BigQuery → dbt staging/intermediate/marts →
dashboard, with Dagster orchestrating the whole thing), exported from the flowchart.
